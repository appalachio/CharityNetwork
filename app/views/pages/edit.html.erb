<% content_for :title, "Editing page" %>

<div class="md:w-2/3 w-full">
  <h1 class="font-bold text-4xl">Editing page</h1>

  <%= render "form", page: @page %>

  <%= link_to "Show this page", @page, class: "btn btn-secondary" %>
  <%= link_to "Back", :back, class: "btn btn-accent" %>
</div>
